# SCT_ML_1

## 📌 Internship Task 01 — Machine Learning (Linear Regression)

### 🏠 Project: House Price Prediction Model

This repository contains my solution for **Task 01** of the Skillcraft Technology Machine Learning Internship.  
The goal is to implement a **linear regression model** to predict house prices based on **square footage, number of bedrooms, and number of bathrooms**.

---

## 📂 Repository Structure
SCT_ML_1/
│
├── main.py                 
├── data/
│   ├── train.csv            # Training dataset (input)
│   └── test.csv             # Test dataset (input)
├── submission_task01.csv    # Predictions generated by main.py on test set
├── analysis_task01.png      # Graphs showing model performance (actual vs predicted, residuals)
└── README.md                # Documentation explaining the project

---

## 🚀 Steps Performed
1. **Data Loading** — Read `train.csv` and `test.csv` from `./data/`.
2. **Feature Selection** — Used only:
   - `GrLivArea` → Square footage  
   - `BedroomAbvGr` → Bedrooms  
   - `FullBath` → Bathrooms  
3. **Data Cleaning** — Filled missing values with median values.
4. **Feature Scaling** — Standardized features using `StandardScaler`.
5. **Model Training** — Applied `LinearRegression` from scikit-learn.
6. **Evaluation** — Calculated RMSE and R² score.
7. **Submission File** — Generated `submission_task01.csv` with predictions.
8. **Visualization** — Created plots:
   - Actual vs Predicted Prices  
   - Residual Plot (errors vs predictions)

---

## 📊 Results
- **Training RMSE:** (fill your value here after running main.py)  
- **Training R² Score:** (fill your value here)  

Outputs:
- `submission_task01.csv` → predictions for test dataset  
- `analysis_task01.png` → diagnostic plots  

---

## ⚙️ How to Run
1. Ensure data files exist:
   - Place `train.csv` and `test.csv` in `./data/`.
2. Install dependencies:
   ```bash
   pip install pandas numpy matplotlib scikit-learn